@model EmployeePortal.Core.DTOs.EmployeeDto

<div class="new-employee-container">
    <div class="new-employee-box">
        <h2>Sign Up</h2>
        <form method="post" asp-action="AddEmployee">
            @Html.AntiForgeryToken()
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="validation-summary-errors">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <p>@error.ErrorMessage</p>
                    }
                </div>
            }
            <div class="input-group">
                <label asp-for="FirstName"></label>
                <input asp-for="FirstName" type="text" />
            </div>
            <div class="input-group">
                <label asp-for="LastName"></label>
                <input asp-for="LastName" type="text" />
            </div>
            <div class="input-group">
                <label asp-for="EmailAddress"></label>
                <input asp-for="EmailAddress" type="email" />
            </div>
            <div class="input-group">
                <label asp-for="MobileNumber"></label>
                <input asp-for="MobileNumber" type="text" />
            </div>
            <div class="input-group">
                <label asp-for="Address"></label>
                <input asp-for="Address" type="text" />
            </div>
            <div class="input-group">
                <label asp-for="Department"></label>
                <input asp-for="Department" type="text" />
            </div>
            <div class="input-group">
                <label asp-for="Password"></label>
                <input asp-for="Password" type="text" />
            </div>
            <div class="input-group">
                <label asp-for="ConfirmPassword"></label>
                <input asp-for="ConfirmPassword" type="text" />
            </div>
            <div class="input-group new-employee">
                <button type="reset" class="login-btn new-emp">Clear</button>
                <button type="submit" class="login-btn new-emp">Submit</button>                
            </div>
        </form>
    </div>
</div>